# Production build configuration
# Real API URL should be set via CDN host environment variables (Vercel/Netlify dashboard)
# Leave empty for Docker/nginx deployments where proxy handles routing
VITE_API_BASE_URL=
VITE_APP_NAME=EHIPAP
VITE_APP_VERSION=1.0.0
